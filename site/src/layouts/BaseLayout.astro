---
import { ViewTransitions } from "astro:transitions";
import "../styles/global.css";
import { profile } from "../data/profile";
import { resources } from "../data/resources";
import { services } from "../data/services";
import { absoluteUrl, SITE_NAME } from "../lib/seo";
import HyperjumpIntro from "../components/HyperjumpIntro.astro";
import {
  ArrowUpRight,
  Copyright,
  Diamond,
  Compass,
  Instagram,
  Mail,
  MapPin,
  MessageCircle,
} from "lucide-react";

type JsonLd = Record<string, unknown>;

interface Props {
  title?: string;
  description?: string;
  path?: string;
  image?: string;
  ogTitle?: string;
  ogDescription?: string;
  ogImageType?: string;
  ogImageWidth?: number;
  ogImageHeight?: number;
  twitterCard?: "summary" | "summary_large_image";
  noindex?: boolean;
  jsonLd?: JsonLd | JsonLd[];
}

const {
  title = `${profile.name} | ${profile.roleTitle}`,
  description = profile.summary,
  path = Astro.url.pathname,
  image = "/og/site.jpg",
  ogTitle = title,
  ogDescription = description,
  ogImageType,
  ogImageWidth,
  ogImageHeight,
  twitterCard = "summary_large_image",
  noindex = false,
  jsonLd,
} = Astro.props;

const siteOrigin = (Astro.site?.toString() || profile.siteUrl).replace(/\/+$/, "");
const canonical = absoluteUrl(path);
const imagePath = image;
const ogImage = imagePath.startsWith("http") ? imagePath : new URL(imagePath, `${siteOrigin}/`).toString();
const ogImageAlt = ogTitle || title;
const isGeneratedOgImage = imagePath.startsWith("/og/") || ogImage.includes("/og/");
const normalizedImagePath = imagePath.split("?")[0].toLowerCase();
const resolvedOgImageType = ogImageType ?? (
  normalizedImagePath.endsWith(".jpg") || normalizedImagePath.endsWith(".jpeg")
    ? "image/jpeg"
    : isGeneratedOgImage
      ? "image/png"
      : undefined
);
const resolvedOgImageWidth = ogImageWidth ?? (isGeneratedOgImage ? 1200 : undefined);
const resolvedOgImageHeight = ogImageHeight ?? (isGeneratedOgImage ? 630 : undefined);
const robotsContent = noindex
  ? "noindex,nofollow"
  : "index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1";
const googleSiteVerification = import.meta.env.PUBLIC_GOOGLE_SITE_VERIFICATION;
const bingSiteVerification = import.meta.env.PUBLIC_BING_SITE_VERIFICATION;
const googleAnalyticsMeasurementId = import.meta.env.PUBLIC_GA_MEASUREMENT_ID || "G-XYR2TYT2QS";
const isDev = import.meta.env.DEV;

const navItems = [
  { href: "/", label: "Home" },
  { href: "/work", label: "Work" },
  { href: "/about", label: "About" },
  { href: "/resume", label: "Resume" },
  { href: "/contact", label: "Contact" },
];
---

<!doctype html>
<html lang="en-AE">
  <head>
    <!-- Critical resource priority chain -->
    <link rel="preload" as="image" href="/profile.png" fetchpriority="high" />

    <!-- Prefetch next likely pages -->
    <link rel="prefetch" href="/work/" />
    <link rel="prefetch" href="/contact/" />

    <!-- DNS prefetch for external services -->
    <link rel="dns-prefetch" href="https://wa.me" />
    <link rel="dns-prefetch" href="https://fonts.googleapis.com" />
    <link rel="dns-prefetch" href="https://cdn.jsdelivr.net" />

    <!-- Preconnect to font CDN -->
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <!-- Preload critical fonts -->
    <link rel="preload" as="font" type="font/woff2"
      href="/fonts/orbitron.woff2" crossorigin />
    <meta charset="UTF-8" />
    <meta http-equiv="x-dns-prefetch-control" content="on" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#07090f" />
    <meta name="author" content={profile.name} />
    <meta name="application-name" content={SITE_NAME} />
    <meta name="apple-mobile-web-app-title" content={SITE_NAME} />
    <meta name="referrer" content="strict-origin-when-cross-origin" />
    {googleSiteVerification && <meta name="google-site-verification" content={googleSiteVerification} />}
    {bingSiteVerification && <meta name="msvalidate.01" content={bingSiteVerification} />}
    {!isDev && (
      <>
        <script async src={`https://www.googletagmanager.com/gtag/js?id=${googleAnalyticsMeasurementId}`}></script>
        <script define:vars={{ googleAnalyticsMeasurementId }}>
          window.dataLayer = window.dataLayer || [];
          function gtag(){window.dataLayer.push(arguments);}
          window.gtag = window.gtag || gtag;
          gtag('js', new Date());
          gtag('config', googleAnalyticsMeasurementId, { send_page_view: false });

          const currentTrackedPath = () => `${window.location.pathname}${window.location.search}${window.location.hash}`;
          const trackPageView = () => {
            const pagePath = currentTrackedPath();
            if (window.__mubaanLastTrackedPath === pagePath) return;
            window.__mubaanLastTrackedPath = pagePath;
            gtag('event', 'page_view', {
              send_to: googleAnalyticsMeasurementId,
              page_title: document.title,
              page_location: window.location.href,
              page_path: pagePath,
            });
          };

          if (document.readyState === 'complete') {
            trackPageView();
          } else {
            window.addEventListener('load', trackPageView, { once: true });
          }

          document.addEventListener('astro:page-load', trackPageView);
        </script>
      </>
    )}
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="robots" content={robotsContent} />
    <meta name="googlebot" content={robotsContent} />
    <link rel="canonical" href={canonical} />
    <link rel="manifest" href="/manifest.json" />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={SITE_NAME} />
    <meta property="og:locale" content="en_AE" />
    <meta property="og:title" content={ogTitle} />
    <meta property="og:description" content={ogDescription} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:url" content={ogImage} />
    <meta property="og:image:secure_url" content={ogImage} />
    {resolvedOgImageType && <meta property="og:image:type" content={resolvedOgImageType} />}
    {resolvedOgImageWidth && <meta property="og:image:width" content={String(resolvedOgImageWidth)} />}
    {resolvedOgImageHeight && <meta property="og:image:height" content={String(resolvedOgImageHeight)} />}
    <meta property="og:image:alt" content={ogImageAlt} />

    <meta name="twitter:card" content={twitterCard} />
    <meta name="twitter:title" content={ogTitle} />
    <meta name="twitter:description" content={ogDescription} />
    <meta name="twitter:image" content={ogImage} />
    <meta name="twitter:image:alt" content={ogImageAlt} />
    <meta name="twitter:creator" content={profile.links.instagramHandle} />
    <meta itemprop="image" content={ogImage} />
    <link rel="image_src" href={ogImage} />

    {jsonLd && <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />}

    <link href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=Orbitron:wght@400;500;600;700;800;900&family=Poppins:wght@500;600&display=swap" rel="stylesheet" />
    <style>
      /* ── CSS VARIABLE STATE SYSTEM ── */
      :root {
        --scroll-y: 0px;
        --scroll-progress: 0;
        --viewport-w: 100vw;
        --viewport-h: 100vh;

        /* Animation easing tokens */
        --ease-out-expo: cubic-bezier(0.22, 1, 0.36, 1);
        --ease-out-back: cubic-bezier(0.34, 1.56, 0.64, 1);
        --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);
        --ease-spring: cubic-bezier(0.34, 1.3, 0.64, 1);

        /* Duration tokens */
        --dur-fast: 0.3s;
        --dur-mid: 0.7s;
        --dur-slow: 1.2s;
        --dur-cinema: 1.6s;

        /* GPU compositing defaults */
        --gpu: none;
      }

      /* ── FORCE GPU LAYERS — NEVER animate margin/padding/width/height ── */
      /* Only transform and opacity are GPU-composited */
      * {
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
      }

      /* All transitions must use ONLY transform + opacity */
      .reveal,
      .metric-card,
      .cap-card,
      .testi-card-v2,
      .work-left,
      .work-card-1,
      .work-card-2,
      .work-card-3,
      .work-card-center,
      .work-card-4 {
        will-change: transform, opacity;
        backface-visibility: hidden;
        -webkit-backface-visibility: hidden;
      }

      /* Scrolling containers: own compositor layer */
      .testi-row,
      .testi-track,
      .ticker-row {
        will-change: transform;
        backface-visibility: hidden;
      }

      /* Hero: isolated paint context */
      .hero-section {
        isolation: isolate;
      }

      /* Sticky header: own layer */
      header, nav {
        backface-visibility: hidden;
      }

      /* ══════════════════════════════════════════
         MUB NAV — expand on hover pill navbar
      ══════════════════════════════════════════ */

      .mub-menu {
        list-style: none;
        display: flex;
        align-items: center;
        gap: 6px;
        padding: 0;
        margin: 0;
        border-radius: 0;
        background: transparent;
        backdrop-filter: none;
        -webkit-backdrop-filter: none;
        border: 0;
        box-shadow: none;
      }

      .mub-item {
        inline-size: 40px;
        transition: inline-size 0.34s cubic-bezier(0.22, 1, 0.36, 1);
        will-change: inline-size;
      }

      .mub-item:hover,
      .mub-item:focus-within,
      .mub-item.mub-item-active {
        inline-size: 118px;
      }

      .mub-link {
        display: flex;
        align-items: center;
        justify-content: flex-start;
        inline-size: 100%;
        gap: 8px;
        padding: 8px 10px;
        border-radius: 999px;
        text-decoration: none;
        color: rgba(148, 163, 184, 0.80);
        cursor: pointer;
        overflow: hidden;
        white-space: nowrap;
        transition:
          background 0.28s ease,
          color 0.22s ease,
          box-shadow 0.24s ease,
          border-color 0.22s ease,
          transform 0.24s ease;
        border: 1px solid transparent;
        will-change: transform, box-shadow;
        backface-visibility: hidden;
        transform: translateZ(0);
      }

      .mub-link:hover,
      .mub-link:focus-visible {
        background: linear-gradient(
          135deg,
          rgba(37, 99, 235, 0.30) 0%,
          rgba(147, 197, 253, 0.15) 100%
        );
        color: #e8edf8;
        gap: 8px;
        border-color: rgba(147, 197, 253, 0.22);
        box-shadow:
          0 0 0 1px rgba(37, 99, 235, 0.20),
          0 4px 20px rgba(37, 99, 235, 0.25),
          inset 0 1px 0 rgba(255, 255, 255, 0.08);
        transform: translateY(-0.5px);
      }

      .mub-link.mub-active {
        background: linear-gradient(
          135deg,
          rgba(37, 99, 235, 0.35) 0%,
          rgba(96, 165, 250, 0.18) 100%
        );
        color: #93c5fd;
        gap: 8px;
        border-color: rgba(37, 99, 235, 0.35);
        box-shadow:
          0 0 0 1px rgba(37, 99, 235, 0.25),
          0 4px 24px rgba(37, 99, 235, 0.20),
          inset 0 1px 0 rgba(255, 255, 255, 0.10);
      }

      .mub-icon {
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        width: 18px;
        height: 18px;
      }

      .mub-icon svg {
        width: 18px;
        height: 18px;
        stroke: currentColor;
        stroke-width: 1.7;
        fill: none;
        stroke-linecap: round;
        stroke-linejoin: round;
        transition: filter 0.22s ease;
      }

      .mub-link:hover .mub-icon svg,
      .mub-link.mub-active .mub-icon svg {
        filter: drop-shadow(0 0 5px rgba(147, 197, 253, 0.7));
      }

      .mub-label {
        opacity: 0;
        transform: translateX(-6px);
        max-width: 0;
        overflow: hidden;
        font-size: 0.78rem;
        font-weight: 600;
        letter-spacing: 0.04em;
        font-family: 'DM Mono', monospace;
        text-transform: uppercase;
        pointer-events: none;
        transition:
          opacity 0.2s ease,
          transform 0.28s cubic-bezier(0.22, 1, 0.36, 1),
          max-width 0.34s cubic-bezier(0.22, 1, 0.36, 1);
      }

      .mub-link:hover .mub-label,
      .mub-link:focus-visible .mub-label,
      .mub-link.mub-active .mub-label {
        opacity: 1;
        transform: translateX(0);
        max-width: 84px;
      }

      .mub-link:focus-visible {
        outline: 2px solid rgba(147, 197, 253, 0.6);
        outline-offset: 2px;
      }

      @media (max-width: 640px) {
        .mub-menu {
          gap: 2px;
          padding: 0;
        }

        .mub-item {
          inline-size: 36px;
        }

        .mub-item:hover,
        .mub-item:focus-within,
        .mub-item.mub-item-active {
          inline-size: 96px;
        }

        .mub-link {
          padding: 7px 8px;
        }
      }

      @media (prefers-reduced-motion: reduce) {
        .mub-item,
        .mub-link,
        .mub-label {
          transition: none !important;
        }
      }

      /* Freeze nav state animation while route swaps to avoid header blink */
      html.nav-route-switching .mub-item,
      html.nav-route-switching .mub-link,
      html.nav-route-switching .mub-label,
      html.nav-route-switching .mub-icon svg {
        transition: none !important;
      }

      /* Canvas elements: GPU rendered */
      canvas {
        will-change: transform;
      }

      @keyframes onlineHeartbeat {
        0%   { box-shadow: 0 0 4px rgba(34,197,94,0.8), 0 0 10px rgba(34,197,94,0.4); opacity: 1; }
        30%  { box-shadow: 0 0 8px rgba(34,197,94,1.0), 0 0 20px rgba(34,197,94,0.7), 0 0 35px rgba(34,197,94,0.3); opacity: 1; }
        50%  { box-shadow: 0 0 4px rgba(34,197,94,0.6), 0 0 10px rgba(34,197,94,0.3); opacity: 0.85; }
        70%  { box-shadow: 0 0 10px rgba(34,197,94,1.0), 0 0 24px rgba(34,197,94,0.8), 0 0 40px rgba(34,197,94,0.35); opacity: 1; }
        100% { box-shadow: 0 0 4px rgba(34,197,94,0.8), 0 0 10px rgba(34,197,94,0.4); opacity: 1; }
      }

      /* ── ANIMATED ICON MICRO-INTERACTIONS ── */
      @keyframes iconPop {
        0%   { transform: scale(0.7) rotate(-10deg); opacity: 0; }
        60%  { transform: scale(1.15) rotate(4deg); }
        100% { transform: scale(1) rotate(0deg); opacity: 1; }
      }

      .lucide {
        transition:
          transform 0.28s cubic-bezier(0.34, 1.5, 0.64, 1),
          filter 0.28s ease,
          opacity 0.28s ease;
        transform-origin: center;
      }

      a:hover .lucide-arrow-right,
      button:hover .lucide-arrow-right {
        transform: translateX(3px);
      }

      a:hover .lucide-arrow-up-right,
      button:hover .lucide-arrow-up-right,
      a:hover .lucide-external-link,
      button:hover .lucide-external-link {
        transform: translate(2px, -2px);
      }

      a:hover .lucide-chevron-down,
      button:hover .lucide-chevron-down {
        transform: translateY(2px);
      }

      a:hover .lucide-message-circle,
      button:hover .lucide-message-circle,
      a:hover .lucide-mail,
      button:hover .lucide-mail,
      a:hover .lucide-instagram,
      button:hover .lucide-instagram,
      a:hover .lucide-map-pin,
      button:hover .lucide-map-pin,
      a:hover .lucide-copyright,
      button:hover .lucide-copyright,
      a:hover .lucide-compass,
      button:hover .lucide-compass {
        transform: scale(1.08);
      }

      /* Icon glow on interactive elements */
      .ring-hover:hover svg,
      .glass-panel:hover svg {
        filter: drop-shadow(0 0 4px rgba(147,197,253,0.6));
      }

      a.inline-flex:hover,
      button:hover {
        box-shadow:
          0 10px 28px rgba(2, 8, 23, 0.28),
          0 0 0 1px rgba(147,197,253,0.08);
      }

      /* Capability card icon glow */
      .cap-icon-shell {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        transition: transform 0.26s cubic-bezier(0.34, 1.5, 0.64, 1), filter 0.26s ease;
        transform-origin: center;
      }

      .cap-icon-motion {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        will-change: transform;
      }

      .cap-card:hover .cap-icon {
        filter: drop-shadow(0 0 8px rgba(147,197,253,0.7));
        transition: filter 0.3s ease;
      }

      .cap-card:hover .cap-icon .cap-icon-shell {
        transform: scale(1.12);
        filter: drop-shadow(0 0 10px rgba(147,197,253,0.65));
      }

      /* Force GPU layer for animated elements — eliminates paint lag */
      .testi-track,
      .ticker-row,
      canvas {
        will-change: transform;
        backface-visibility: hidden;
        -webkit-backface-visibility: hidden;
      }

      /* Smooth scroll — hardware accelerated */
      html {
        scroll-behavior: smooth;
        -webkit-overflow-scrolling: touch;
        text-rendering: optimizeSpeed;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
      }

      /* Prevent layout shift during font load */
      body {
        font-display: swap;
      }

      /* Eliminate paint on scroll for fixed/sticky elements */
      header {
        will-change: transform;
        transform: translateZ(0);
        contain: paint;
      }
      /* ── SPLASH SCREEN ── */
      @keyframes splashBeam {
        0%   { opacity: 0; transform: translateX(-50%) scaleY(0); transform-origin: top; }
        60%  { opacity: 1; transform: translateX(-50%) scaleY(1); }
        100% { opacity: 0.3; transform: translateX(-50%) scaleY(1); }
      }

      @keyframes splashProgress {
        0%   { width: 0%; }
        60%  { width: 75%; }
        85%  { width: 92%; }
        100% { width: 100%; }
      }

      @keyframes splashShimmer {
        0%   { background-position: 200% center; }
        100% { background-position: -200% center; }
      }

      @keyframes letterFill {
        0%   {
          color: transparent;
          -webkit-text-stroke: 1px rgba(147,197,253,0.25);
          text-shadow: none;
        }
        40%  {
          color: transparent;
          -webkit-text-stroke: 1px rgba(147,197,253,0.6);
          text-shadow: 0 0 20px rgba(37,99,235,0.5);
        }
        100% {
          color: #e8edf8;
          -webkit-text-stroke: 0px transparent;
          text-shadow:
            0 0 30px rgba(147,197,253,0.25),
            0 0 60px rgba(37,99,235,0.15);
        }
      }

      @keyframes splashFadeOut {
        0%   { opacity: 1; transform: scale(1); }
        100% { opacity: 0; transform: scale(1.015); }
      }

      #splash.splash-exit {
        animation: splashFadeOut 0.7s cubic-bezier(0.22,1,0.36,1) forwards;
        pointer-events: none;
      }

      /* Prevent body scroll while intro is playing */
      body.splash-active,
      body.hyperjump-active {
        overflow: hidden;
        height: 100vh;
      }

      /* Force 60fps GPU compositing on all animated elements */
      .orbit-dot,
      .orbit-ring,
      .testi-track,
      .ticker-row,
      canvas {
        -webkit-backface-visibility: hidden;
        backface-visibility: hidden;
      }

      /* Prevent subpixel rendering lag on text animations */
      .reveal,
      .metric-card p,
      .cap-card p {
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        text-rendering: optimizeSpeed;
      }

      /* Contain paint to reduce repaint area */
      .hero-section {
        isolation: isolate;
      }

      /* ── VIEW TRANSITIONS — keep header static; swipe content only ── */
      ::view-transition-old(root) {
        animation: none;
      }
      ::view-transition-new(root) {
        animation: none;
      }

      ::view-transition-group(page-content) {
        animation-duration: 0.42s;
        animation-timing-function: cubic-bezier(0.22, 1, 0.36, 1);
      }

      ::view-transition-old(page-content) {
        animation: pageSwipeOut 0.3s cubic-bezier(0.22, 1, 0.36, 1) both;
      }

      ::view-transition-new(page-content) {
        animation: pageSwipeIn 0.42s cubic-bezier(0.22, 1, 0.36, 1) both;
      }

      @keyframes pageSwipeOut {
        from { opacity: 1; transform: translate3d(0, 0, 0) scale(1); }
        to   { opacity: 0.08; transform: translate3d(-42px, 0, 0) scale(0.995); }
      }
      @keyframes pageSwipeIn {
        from { opacity: 0; transform: translate3d(42px, 0, 0) scale(1.005); }
        to   { opacity: 1; transform: translate3d(0, 0, 0) scale(1); }
      }

      @media (prefers-reduced-motion: reduce) {
        ::view-transition-old(page-content),
        ::view-transition-new(page-content) {
          animation: none;
        }
      }
    </style>
    <ViewTransitions fallback="none" />
  </head>
    <body class="antialiased">
    <!-- Galaxy Hyperjump Intro overlay -->
    <HyperjumpIntro />

    <!-- Noise overlay -->
    <div class="noise-overlay" aria-hidden="true"></div>



    <!-- Cursor -->
    <div id="cursor-dot" aria-hidden="true"></div>
    <div id="cursor-ring" aria-hidden="true"></div>

    <!-- NAV -->
    <header transition:persist class="fixed inset-x-0 top-0 z-50" style="backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px); background: rgba(7,9,15,0.28); border-bottom: 1px solid rgba(255,255,255,0.04);">

      <!-- Top micro-bar: location left, name center, status right -->
      <div class="mx-auto max-w-7xl px-6 grid grid-cols-[1fr_auto_1fr] items-center h-9 border-b" style="border-color: rgba(255,255,255,0.04);">
        <span class="text-xs font-mono tracking-widest uppercase hidden sm:block justify-self-start" style="color: rgba(147,197,253,0.45);">
          {profile.location}
          <Diamond size={9} strokeWidth={1.8} className="inline-block mx-2 align-[-1px]" />
          Available for mandates
        </span>
        <a href="/" class="justify-self-center" style="padding-left: 4px;">
          <div class="flex items-center gap-2 sm:hidden">
            <span class="hero-online-dot" style="
              width: 7px; height: 7px; border-radius: 50%; flex-shrink: 0;
              background: #22c55e;
              box-shadow: 0 0 6px rgba(34,197,94,0.9), 0 0 14px rgba(34,197,94,0.5);
              animation: onlineHeartbeat 2.4s ease-in-out infinite;
              display: inline-block;">
            </span>
            <p class="text-xs font-mono tracking-[0.22em] uppercase text-slate-300 whitespace-nowrap">
              {profile.name}
            </p>
          </div>
          <span class="hidden text-xs font-mono tracking-[0.32em] uppercase sm:inline-block" style="color: rgba(224,237,248,0.55);">
            {profile.name}
          </span>
        </a>
        <div class="hidden items-center gap-1.5 justify-self-end sm:flex">
          <div class="w-1.5 h-1.5 rounded-full bg-green-400" style="box-shadow: 0 0 6px rgba(74,222,128,0.6); opacity: 0.8;"></div>
          <span class="text-xs font-mono tracking-widest uppercase hidden sm:block" style="color: rgba(147,197,253,0.40);">Open to work</span>
        </div>
      </div>

      <!-- Main nav bar -->
      <div class="mx-auto max-w-7xl px-6 grid grid-cols-[1fr_auto_1fr] items-center h-12">

        <!-- Left: subtle home link only -->
        <a href="/" class="flex items-center group justify-self-start" aria-label="Mubaan home">
          <span class="sr-only">Mubaan home</span>
          <span class="items-center justify-center transition-colors duration-200 hidden lg:inline-flex"
            style="color: rgba(147,197,253,0.34); width: 18px; height: 18px;"
            onmouseover="this.style.color='rgba(147,197,253,0.65)'"
            onmouseout="this.style.color='rgba(147,197,253,0.34)'">
            <Compass size={14} strokeWidth={1.9} className="inline-block shrink-0" />
          </span>
        </a>

        <!-- Center: nav links -->
        <nav class="justify-self-center" aria-label="Primary">
          <ul class="mub-menu" role="list">
            <li class="mub-item">
              <a href="/" class="mub-link" aria-label="Home" data-active-path="/">
                <span class="mub-icon">
                  <svg viewBox="0 0 24 24"><path d="M3 9.5L12 3l9 6.5V20a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1V9.5z"></path><polyline points="9 21 9 12 15 12 15 21"></polyline></svg>
                </span>
                <span class="mub-label">Home</span>
              </a>
            </li>

            <li class="mub-item">
              <a href="/work/" class="mub-link" aria-label="Work" data-active-path="/work">
                <span class="mub-icon">
                  <svg viewBox="0 0 24 24"><rect x="2" y="7" width="20" height="14" rx="2"></rect><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"></path><line x1="12" y1="12" x2="12" y2="16"></line><line x1="10" y1="14" x2="14" y2="14"></line></svg>
                </span>
                <span class="mub-label">Work</span>
              </a>
            </li>

            <li class="mub-item">
              <a href="/about/" class="mub-link" aria-label="About" data-active-path="/about">
                <span class="mub-icon">
                  <svg viewBox="0 0 24 24"><circle cx="12" cy="8" r="4"></circle><path d="M4 20c0-4 3.6-7 8-7s8 3 8 7"></path></svg>
                </span>
                <span class="mub-label">About</span>
              </a>
            </li>

            <li class="mub-item">
              <a href="/resume/" class="mub-link" aria-label="Resume" data-active-path="/resume">
                <span class="mub-icon">
                  <svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path><polyline points="14 2 14 8 20 8"></polyline><line x1="8" y1="13" x2="16" y2="13"></line><line x1="8" y1="17" x2="13" y2="17"></line></svg>
                </span>
                <span class="mub-label">Resume</span>
              </a>
            </li>

            <li class="mub-item">
              <a href="/contact/" class="mub-link" aria-label="Contact" data-active-path="/contact">
                <span class="mub-icon">
                  <svg viewBox="0 0 24 24"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg>
                </span>
                <span class="mub-label">Contact</span>
              </a>
            </li>
          </ul>
        </nav>

        <!-- Right: WhatsApp CTA -->
        <a
          href={profile.links.whatsapp}
          target="_blank"
          rel="noreferrer"
        class="hidden sm:inline-flex items-center gap-1.5 rounded-full px-4 py-1.5 text-xs font-medium font-mono tracking-wider transition-all duration-200 hover:scale-[1.03] justify-self-end"
        style="background: rgba(37,99,235,0.20); border: 1px solid rgba(37,99,235,0.40); color: #93c5fd;"
        >
          <ArrowUpRight size={13} strokeWidth={2.2} className="inline-block shrink-0" />
          WhatsApp
        </a>
      </div>

    </header>

    <!-- MAIN -->
    <main transition:name="page-content" class="flex-1 pt-[88px] container-shell">
      <slot />
    </main>

    <!-- FOOTER -->
    <footer class="container-shell mt-16 px-4 pb-10">
      <div class="mx-auto max-w-5xl glass-panel rounded-[1.75rem] px-6 py-7 md:px-7 md:py-8">
        <div class="grid gap-8 md:grid-cols-2 xl:grid-cols-[minmax(0,1.15fr)_minmax(0,1fr)_minmax(0,0.92fr)_minmax(0,1.08fr)] xl:items-start">

          <!-- Identity -->
          <div class="space-y-4">
            <p class="font-display text-base font-semibold text-white">{profile.name}</p>
            <p class="text-sm text-accent-soft">{profile.roleTitle}</p>
            <p class="max-w-xs text-sm leading-7 text-slate-400">
              Digital marketing, technical SEO, GEO, and web systems built for growth clarity.
            </p>
            <p class="text-xs text-slate-500">{profile.location}</p>
            <div class="flex flex-wrap gap-2 pt-1">
              <a href="/work/"
                class="inline-flex items-center rounded-full border border-white/8 bg-white/4 px-3 py-1.5 text-xs font-mono text-slate-400 transition-colors hover:text-white hover:border-white/14">
                Dubai Case Studies
              </a>
              <a href="/services/"
                class="inline-flex items-center rounded-full border border-white/8 bg-white/4 px-3 py-1.5 text-xs font-mono text-slate-400 transition-colors hover:text-white hover:border-white/14">
                SEO & Web Services
              </a>
              <a href="/resources/"
                class="inline-flex items-center rounded-full border border-white/8 bg-white/4 px-3 py-1.5 text-xs font-mono text-slate-400 transition-colors hover:text-white hover:border-white/14">
                SEO & GEO Guides
              </a>
            </div>
          </div>

          <!-- Links -->
          <div class="space-y-3">
            <p class="text-xs uppercase tracking-widest text-slate-600 mb-3">Connect</p>
            <a href={profile.siteUrl}
              class="flex items-center gap-2 text-sm text-slate-400 hover:text-accent-soft transition-colors">
              <Compass size={14} strokeWidth={2} className="inline-block shrink-0" />
              {profile.siteUrl.replace(/^https?:\/\//, "")}
            </a>
            <a href={profile.links.whatsapp} target="_blank" rel="noreferrer"
              class="flex items-center gap-2 text-sm text-slate-400 hover:text-accent-soft transition-colors">
              <MessageCircle size={14} strokeWidth={2} className="inline-block shrink-0" />
              WhatsApp &middot; {profile.whatsappDisplay}
            </a>
            <a href={profile.links.email}
              class="flex items-center gap-2 text-sm text-slate-400 hover:text-accent-soft transition-colors">
              <Mail size={14} strokeWidth={2} className="inline-block shrink-0" />
              {profile.email}
            </a>
            <a href={profile.links.instagram} target="_blank" rel="noreferrer me"
              class="flex items-center gap-2 text-sm text-slate-400 hover:text-accent-soft transition-colors">
              <Instagram size={14} strokeWidth={2} className="inline-block shrink-0" />
              {profile.links.instagramHandle}
            </a>
          </div>

          <div class="space-y-2">
            <p class="text-xs uppercase tracking-widest text-slate-600 mb-3">Navigate</p>
            {navItems.map((item) => (
              <a href={item.href}
                class="block text-sm text-slate-400 hover:text-white transition-colors">
                {item.label}
              </a>
            ))}
            <div class="pt-4 mt-4 border-t border-white/6 space-y-2">
              <p class="text-[11px] uppercase tracking-[0.24em] text-slate-600 mb-3">Services</p>
              <a href="/services/" class="block text-sm text-slate-400 hover:text-white transition-colors">Dubai SEO, web development & AI services</a>
              {services.slice(0, 4).map((service) => (
                <a href={`/services/${service.slug}/`}
                  class="block text-sm text-slate-400 hover:text-white transition-colors">
                  {service.title}
                </a>
              ))}
            </div>
          </div>

          <div class="space-y-5">
            <div class="space-y-2">
              <p class="text-[11px] uppercase tracking-[0.24em] text-slate-600 mb-3">Guides</p>
              <a href="/dubai-digital-marketer/" class="block text-sm text-slate-400 hover:text-white transition-colors">Dubai digital marketer profile</a>
              <a href="/freelance-digital-marketer-dubai/" class="block text-sm text-slate-400 hover:text-white transition-colors">Freelance digital marketer Dubai</a>
              <a href="/dubai-web-developer/" class="block text-sm text-slate-400 hover:text-white transition-colors">Dubai web developer profile</a>
              <a href="/dubai-ai-engineer/" class="block text-sm text-slate-400 hover:text-white transition-colors">Dubai AI engineer and GEO specialist</a>
              <a href="/generative-engine-optimization-specialist-dubai/" class="block text-sm text-slate-400 hover:text-white transition-colors">Generative engine optimization specialist in Dubai</a>
            </div>
            <div class="space-y-2 pt-4 border-t border-white/6">
              <p class="text-[11px] uppercase tracking-[0.24em] text-slate-600 mb-3">Resources</p>
              <a href="/press-kit/" class="block text-sm text-slate-400 hover:text-white transition-colors">Press Kit</a>
              <a href="/resources/" class="block text-sm text-slate-400 hover:text-white transition-colors">Dubai SEO & GEO resources</a>
              {resources.slice(0, 3).map((resource) => (
                <a href={`/resources/${resource.slug}/`}
                  class="block text-sm text-slate-400 hover:text-white transition-colors">
                  {resource.title}
                </a>
              ))}
            </div>
          </div>
        </div>

        <div class="mt-6 pt-5 border-t border-white/6 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
          <p class="text-xs text-slate-600 flex items-center gap-1.5">
            <Copyright size={12} strokeWidth={1.8} className="inline-block shrink-0" />
            <span>{new Date().getFullYear()} {profile.name} · All rights reserved.</span>
          </p>
          <p class="text-xs text-slate-700 font-mono flex items-center gap-1.5">
            <MapPin size={12} strokeWidth={1.8} className="inline-block shrink-0" />
            <span>Dubai, UAE · Available for new mandates</span>
          </p>
        </div>
      </div>
    </footer>

    <!-- Cursor JS -->
    <script is:inline>
      (() => {
        if (window.__mubCursorInit) return;

        function initCursor() {
        const supportsFinePointer = window.matchMedia('(hover: hover) and (pointer: fine)').matches;
        const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        if (!supportsFinePointer || prefersReducedMotion) {
          window.__mubCursorInit = true;
          document.documentElement.classList.add('cursor-disabled');
          return;
        }

        const dot = document.getElementById("cursor-dot");
        const ring = document.getElementById("cursor-ring");
        if (!dot || !ring) return;
        window.__mubCursorInit = true;

        let mx = -100, my = -100, rx = -100, ry = -100;
        let rafId;

        // Initialize positions to 0 so transform works properly
        dot.style.left = "0px";
        dot.style.top = "0px";
        ring.style.left = "0px";
        ring.style.top = "0px";

        document.addEventListener("mousemove", (e) => {
          mx = e.clientX; my = e.clientY;
          dot.style.transform = `translate3d(${mx}px, ${my}px, 0)`;
        });

        const animateRing = () => {
          rx += (mx - rx) * 0.12;
          ry += (my - ry) * 0.12;
          ring.style.transform = `translate3d(${rx}px, ${ry}px, 0)`;
          rafId = requestAnimationFrame(animateRing);
        };
        animateRing();

        document.querySelectorAll("a, button, [role=button], input, textarea, select").forEach((el) => {
          el.addEventListener("mouseenter", () => document.body.classList.add("cursor-hover"));
          el.addEventListener("mouseleave", () => document.body.classList.remove("cursor-hover"));
        });

        document.addEventListener("mousedown", () => document.body.classList.add("cursor-clicking"));
        document.addEventListener("mouseup", () => document.body.classList.remove("cursor-clicking"));
        document.addEventListener("mouseleave", () => { dot.style.opacity = "0"; ring.style.opacity = "0"; });
        document.addEventListener("mouseenter", () => { dot.style.opacity = "1"; ring.style.opacity = "1"; });
        } // end initCursor
        if ('requestIdleCallback' in window) {
          requestIdleCallback(initCursor, { timeout: 1000 });
        } else {
          setTimeout(initCursor, 200);
        }
      })();
    </script>

    <!-- Reveal observer -->
    <script is:inline>
    (() => {
      const readQueue = [];
      const writeQueue = [];
      let observer = null;
      const revealTimers = new Set();

      function flushQueues() {
        readQueue.forEach((fn) => fn());
        readQueue.length = 0;
        writeQueue.forEach((fn) => fn());
        writeQueue.length = 0;
      }

      function clearRevealObserver() {
        if (observer) {
          observer.disconnect();
          observer = null;
        }

        revealTimers.forEach((timerId) => window.clearTimeout(timerId));
        revealTimers.clear();
      }

      function queueReveal(el, delay) {
        const timerId = window.setTimeout(() => {
          revealTimers.delete(timerId);
          if (!document.body.contains(el)) return;
          el.classList.add('revealed');
        }, delay);
        revealTimers.add(timerId);
      }

      function triggerReveal(el, delay, isIntersecting) {
        if (!isIntersecting || el.classList.contains('revealed')) return;

        if (document.body.classList.contains('hyperjump-active')) {
          document.addEventListener('hyperjump-complete', () => {
            queueReveal(el, delay);
          }, { once: true });
          return;
        }

        queueReveal(el, delay);
      }

      function initRevealObserver() {
        clearRevealObserver();

        const els = Array.from(document.querySelectorAll('.reveal')).filter(
          (el) => !el.closest('.hero-section')
        );

        if (!els.length) return;

        observer = new IntersectionObserver((entries) => {
          entries.forEach((entry) => {
            readQueue.push(() => {
              const delay = parseFloat(entry.target.dataset.delay || '0') * 1000;
              const el = entry.target;
              const isIntersecting = entry.isIntersecting;

              writeQueue.push(() => {
                triggerReveal(el, delay, isIntersecting);
              });
            });
          });

          requestAnimationFrame(flushQueues);
        }, { threshold: 0.18, rootMargin: '0px 0px -60px 0px' });

        els.forEach((el) => observer.observe(el));
      }

      if (!window.__layoutRevealObserverBound) {
        document.addEventListener('astro:page-load', initRevealObserver);
        document.addEventListener('astro:after-swap', clearRevealObserver);
        window.__layoutRevealObserverBound = true;
      }

      window.__layoutRevealObserverCleanup = clearRevealObserver;
      window.__layoutRevealObserverInit = initRevealObserver;
      initRevealObserver();
    })();
    </script>





    <script is:inline define:vars={{ isDev }}>
    (() => {
      if (!('serviceWorker' in navigator)) return;

      const isLocalHost = ['localhost', '127.0.0.1', '::1'].includes(window.location.hostname);

      if (isDev || isLocalHost) {
        window.addEventListener('load', () => {
          navigator.serviceWorker.getRegistrations()
            .then((registrations) => Promise.all(registrations.map((registration) => registration.unregister())))
            .catch(() => {});

          if ('caches' in window) {
            caches.keys()
              .then((keys) => Promise.all(keys.map((key) => caches.delete(key))))
              .catch(() => {});
          }
        }, { once: true });
        return;
      }

      window.addEventListener('load', () => {
        navigator.serviceWorker.register('/sw.js').catch(() => {});
      }, { once: true });
    })();
    </script>

    <!-- Preload all pages after first paint — instant navigation -->
    <script is:inline>
    (() => {
      if (window.__mubIdlePrefetchInit) return;
      window.__mubIdlePrefetchInit = true;

      const connection = navigator.connection || navigator.mozConnection || navigator.webkitConnection;
      const saveData = Boolean(connection && connection.saveData);
      const effectiveType = connection && connection.effectiveType ? connection.effectiveType : "";
      const isSlowConnection = ["slow-2g", "2g"].includes(effectiveType);
      const isCoarsePointer = window.matchMedia('(pointer: coarse)').matches || window.matchMedia('(hover: none)').matches;

      if (saveData || isSlowConnection) return;

      function prefetchAll() {
        const pages = isCoarsePointer
          ? ['/work/', '/contact/']
          : ['/work/', '/about/', '/contact/', '/resume/'];

        const schedule = window.requestIdleCallback || ((cb) => setTimeout(cb, 600));

        schedule(() => {
          pages.forEach(href => {
            const existing = document.querySelector(`link[href="${href}"][rel="prefetch"]`);
            if (existing) return;
            const link = document.createElement('link');
            link.rel = 'prefetch';
            link.href = href;
            link.as = 'document';
            document.head.appendChild(link);
          });
        }, { timeout: 2000 });
      }

      if (document.readyState === 'complete') {
        prefetchAll();
      } else {
        window.addEventListener('load', prefetchAll);
      }
    })();
    </script>

    <script is:inline>
    (() => {
      if (window.__mubIntentPrefetchInit) return;
      window.__mubIntentPrefetchInit = true;

      const connection = navigator.connection || navigator.mozConnection || navigator.webkitConnection;
      const saveData = Boolean(connection && connection.saveData);
      const effectiveType = connection && connection.effectiveType ? connection.effectiveType : "";
      const isSlowConnection = ["slow-2g", "2g"].includes(effectiveType);
      const supportsHover = window.matchMedia('(hover: hover) and (pointer: fine)').matches;
      if (saveData || isSlowConnection) return;

      const prefetched = new Set();
      function prefetchHref(href) {
        if (!href || !href.startsWith('/')) return;
        if (href.startsWith('//') || href.startsWith('/#')) return;
        if (prefetched.has(href)) return;
        if (document.querySelector(`link[href="${href}"][rel="prefetch"]`)) {
          prefetched.add(href);
          return;
        }
        const pl = document.createElement('link');
        pl.rel = 'prefetch';
        pl.href = href;
        pl.as = 'document';
        document.head.appendChild(pl);
        prefetched.add(href);
      }

      if (supportsHover) {
        document.addEventListener('mouseover', (e) => {
          const link = e.target.closest('a[href]');
          if (!link || link.target === '_blank') return;
          prefetchHref(link.getAttribute('href'));
        }, { passive: true });
      }

      document.addEventListener('focusin', (e) => {
        const link = e.target.closest && e.target.closest('a[href]');
        if (!link || link.target === '_blank') return;
        prefetchHref(link.getAttribute('href'));
      });
    })();
    </script>

    <script is:inline>
    (() => {
      const applyActiveNav = () => {
        const path = window.location.pathname.replace(/\/$/, '') || '/';
        document.querySelectorAll('.mub-link[data-active-path]').forEach((link) => {
          const linkPath = (link.getAttribute('data-active-path') || '').replace(/\/$/, '') || '/';
          const isActive = path === linkPath;
          link.classList.toggle('mub-active', isActive);
          const item = link.closest('.mub-item');
          if (item) item.classList.toggle('mub-item-active', isActive);
        });
      };

      const freezeNavForSwap = () => {
        document.documentElement.classList.add('nav-route-switching');
      };

      const unfreezeNavAfterSwap = () => {
        requestAnimationFrame(() => {
          requestAnimationFrame(() => {
            document.documentElement.classList.remove('nav-route-switching');
          });
        });
      };

      if (!window.__mubNavBindingsAdded) {
        document.addEventListener('astro:before-swap', freezeNavForSwap);
        document.addEventListener('astro:page-load', () => {
          applyActiveNav();
          unfreezeNavAfterSwap();
        });
        window.__mubNavBindingsAdded = true;
      }

      applyActiveNav();
    })();
    </script>
  </body>
</html>
